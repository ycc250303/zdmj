<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zdmj.resumeService.mapper.EducationMapper">

    <select id="selectByUserId" resultType="com.zdmj.resumeService.entity.Education">
        SELECT id, user_id, school, major, degree, start_date, end_date, gpa, created_at, updated_at
        FROM educations
        WHERE user_id = #{userId}
        ORDER BY start_date ASC
    </select>
</mapper>
