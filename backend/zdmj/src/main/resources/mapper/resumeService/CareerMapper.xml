<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zdmj.resumeService.mapper.CareerMapper">

    <select id="selectByUserId" resultType="com.zdmj.resumeService.entity.Career">
        SELECT id, user_id, company, position, start_date, end_date, details, created_at, updated_at
        FROM careers
        WHERE user_id = #{userId}
        ORDER BY start_date DESC
    </select>
</mapper>
