<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zdmj.resumeService.mapper.ResumeMapper">
    <!-- 复用 MP autoResultMap（含 List<Long> 的 JacksonTypeHandler Field 构造器） -->
    <select id="selectByUserId" resultMap="mybatis-plus_Resume">
        SELECT id, user_id, skill_id, projects, careers, educations, awards, resume_matched_ids,
               created_at, updated_at
        FROM resumes WHERE user_id = #{userId} ORDER BY created_at DESC
    </select>

    <select id="selectOneByUserId" resultMap="mybatis-plus_Resume">
        SELECT id, user_id, skill_id, projects, careers, educations, awards, resume_matched_ids,
               created_at, updated_at
        FROM resumes
        WHERE user_id = #{userId}
        LIMIT 1
    </select>

    <select id="existsByUserId" resultType="boolean">
        SELECT COUNT(*) > 0 FROM resumes WHERE user_id = #{userId}
    </select>
</mapper>
