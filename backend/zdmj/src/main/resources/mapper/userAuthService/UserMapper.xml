<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zdmj.userAuthService.mapper.UserMapper">
    <!-- 
        注意：所有基础 CRUD 和条件查询都已由 MyBatis-Plus 的 BaseMapper 和 QueryWrapper 提供
        无需在此 XML 中定义任何 SQL，MyBatis-Plus 会自动生成对应的 SQL
        
        例如：
        - selectById(id) - 自动生成
        - selectOne(wrapper) - 自动生成
        - selectCount(wrapper) - 自动生成
        - update(wrapper) - 自动生成
    -->
</mapper>
