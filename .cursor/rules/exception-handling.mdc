---
description: 后端异常处理与 RFC 9457 Problem Details、业务错误码约定
globs: backend/zdmj/**/*.java
alwaysApply: false
---

# 异常处理规则

代码位于 `com.zdmj.common.exception`，采用 **RFC 9457 Problem Details + 业务错误码**。

## 组件职责

- **`ErrorCode`**：枚举绑定 `code`、`message`、`HttpStatus`；`0` 成功
- **`BusinessException`**：业务异常，优先 `new BusinessException(ErrorCode.XXX)`
- **`ProblemDetailSupport`**：构建 `ProblemDetail`，业务码写入 `properties.code`
- **`GlobalExceptionHandler`**：业务异常、校验、JSON 解析、文件超限等
- **`UncaughtExceptionAdvice`**：兜底未捕获异常 → `SYSTEM_EXCEPTION`（1008）

## 错误码分段

`1xxx` 通用 · `2xxx` 用户 · `3xxx` 简历/能力画像 · `4xxx`–`7xxx` 各类经历 · `8xxx` 知识库 · `9xxx` 对话 · `100xx` 岗位 · `110xx` 人岗匹配 · `120xx` 职业发展报告

## 编码约定

1. 业务失败：`throw new BusinessException(ErrorCode.XXX)`，Controller 不手动拼错误响应
2. 新增错误码：在对应域段追加，**必须显式绑定 `HttpStatus`**
3. HTTP 状态与业务码分离；客户端读 `properties.code`
4. 校验失败 → `VALIDATION_ERROR`（1001），detail 含字段信息
5. 未知异常：记录 error 日志，对外 500 + `SYSTEM_EXCEPTION`，不泄露堆栈
6. 成功：Controller 返回 `Result.success(data)`，`code = 0`

响应示例（`Content-Type: application/problem+json`）：

```json
{ "title": "简历不存在", "status": 404, "detail": "简历不存在", "code": 3006 }
```
