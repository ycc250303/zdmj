---
description: 临时任务 — Entity 出参收口，Controller 直传 entity 时须主动提醒用户
alwaysApply: true
---

# 临时任务（Entity 出参收口）

> **状态**：进行中；相关接口 DTO 改造完成后删除本规则文件，并移除 `AGENTS.md` 中的对应索引。

## Agent 须主动执行

在以下任一情况出现时，**须向用户主动说明**当前接口仍直传 `entity`，并**询问是否需要**新增或改用出参类（`XxxDTO` / `XxxResp` 等，字段与现有 JSON 保持一致）：

1. **新增或修改** Controller，且 `Result<T>` 的 `T` 为 `entity` 包下的类
2. **联调 / 审查**时扫到仍返回 Entity 的既有接口
3. 用户任务涉及下表中的模块或类似 CRUD 接口

**禁止**未经确认批量把 Entity 改成 DTO（涉及前端 JSON 契约与映射工作量）。

## 当前仍直传 Entity 的接口（参考）

`ResumeController` → `Resume`；`JobController` → `Job`；`ConversationController` → `Conversation`；经历类 Controller → 对应 Entity；知识库 Controller → `KnowledgeDocument` / `KnowledgeBases`。已用 DTO 的接口（如 `JobListItemDTO`）无需提醒。

## 改造时注意

- 出参镜像 **当前 API JSON**，放 `{domain}Service/dto/`，勿未经确认批量改 DTO
